用語
UNIXが師匠でLinuxが弟子、UNIXを参考にして作られたのがLinux（狭義はカーネル部分を指す）
絶対パス＝フルパスのこと
相対パス＝現在のディレクトリから目的のファイルやフォルダまで。
ディレクトリとはGUIのフォルダのこと


pwd	print(current) working directory（絶対パス表示）
ls	現在のディレクトリに含まれるファイル名をリスト表示(「ls -l」で所有者情報等、「ls -a」で隠しファイル等）
cd 	cd (ディレクトリ名)　　チェンジ・ディレクトリ

